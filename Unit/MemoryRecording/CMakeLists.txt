register_concrete (MemoryRecording)
concrete_include (PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}")
concrete_sources ("*.cpp")
concrete_require (SCOPE PRIVATE CONCRETE_INTERFACE Serialization)
concrete_require (SCOPE PUBLIC ABSTRACT Log Memory MemoryProfiler CONCRETE_INTERFACE Container INTERFACE APICommon)
