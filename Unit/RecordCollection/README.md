# RecordCollection<sup>Abstract</sup>

RecordCollection abstract unit provides storage for records of the same type and allows creating representations to 
speedup record access using parametrized lookups.
