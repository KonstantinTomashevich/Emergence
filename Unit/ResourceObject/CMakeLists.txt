register_concrete (ResourceObject)
concrete_include (PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}")
concrete_sources ("*.cpp")
concrete_require (SCOPE PRIVATE ABSTRACT Log)
concrete_require (SCOPE PUBLIC ABSTRACT ResourceProvider CONCRETE_INTERFACE Container SyntaxSugar)
